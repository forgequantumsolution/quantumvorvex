# Skip Puppeteer's bundled-Chromium download — we drive an installed Chrome
# instead (see src/utils/pdf.js). Set PUPPETEER_EXECUTABLE_PATH / CHROME_PATH
# if Chrome is not in a standard location. In an environment with no Chrome
# present, remove this line so Puppeteer downloads its own browser.
puppeteer_skip_download=true
